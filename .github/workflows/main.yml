name: Deploy Website

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Copy files to the server
        run: scp -r ./index.html ubuntu@51.20.115.169:/var/www/html
